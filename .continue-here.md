---
phase: ad-hoc — Footer selection feature
task: testing
status: paused — implementation done, needs browser verification
last_updated: 2026-03-24T12:19:55.030Z
---

<current_state>
Footer selection feature is fully implemented and type-checks clean (`npm run lint` passes). NOT yet browser-tested in Word. The generated .docx files with footer pages need manual verification.
</current_state>

<completed_work>

- **Split Stopki.docx** → 3 files using PizZip + node script (section boundary XML splitting):
  - `Stopka_jagienka.docx` — Jagienka Pieprzowska
  - `Stopka_alicja.docx` — Alicja Kozłowska
  - `Stopka_agata.docx` — Agata Truszkowska
  - All 3 copied to `public/` alongside `SZABLON2.docx`

- **`appendFooterToZip(mainZip, footerBuffer)`** added to `src/App.tsx`:
  - Extracts footer body XML from the person's docx
  - Remaps all rIds (image embeds + hyperlinks) to `rIdFtr_{original}` to avoid collisions
  - Copies footer image into `word/media/footer_{rId}.{ext}` in the main zip
  - Adds hyperlink rels with TargetMode="External"
  - Converts main doc's final `<w:sectPr>` into a section-break paragraph
  - Appends footer body (which ends with its own `<w:sectPr>`) → clean page break

- **`generateDocxBlob` + `generateMergedOfferBlob`** now accept `footerBuffer: ArrayBuffer` and call `appendFooterToZip` before returning

- **UI**: `FooterSelector` component with Jagienka / Alicja / Agata toggle buttons:
  - Shown in preview header (single export)
  - Shown in batch toolbar when products selected
  - All 3 export buttons disabled until footer selected
  - `selectedFooter` is global state — one choice for all exports

</completed_work>

<remaining_work>

- **Test in browser**: `npm run dev` → upload `baza danych.xlsx` → select product → pick footer → download → open in Word
- **Verify**: footer page 2 shows the person's photo + contact info with correct layout
- **Test batch**: multi-select → footer → export separately (zip) → open each docx in Word
- **Test merged offer**: multi-select → footer → "Jako ofertę" → verify footer on last page
- **Commit** all changes once tests pass

</remaining_work>

<decisions_made>

- Persons from Stopki.docx: Jagienka Pieprzowska, Alicja Kozłowska, Agata Truszkowska (user said "kasia etc" but the actual file has these 3 — confirm with user if a Kasia file is needed separately)
- Footer merge via direct XML section insertion (not HTML altChunk) — preserves Word formatting, images, fonts
- Global footer state: one selection applies to all exports in a session

</decisions_made>

<blockers>

- None technical. Needs human testing in Word to confirm the XML manipulation renders correctly.

</blockers>

<context>
The `appendFooterToZip` function is the key piece. It splits the main document into 2 Word sections: section 1 = product content (original sectPr wrapped in a break paragraph), section 2 = footer page (with the person's sectPr as the document's final sectPr). The rId remapping avoids any collision between the main doc's rIds (rId1–rId18 range in SZABLON2) and the footer's image rId (rId244/245/246).
</context>

<next_action>
Start with: `npm run dev` → upload `baza danych.xlsx` → select any product → click "Jagienka" → click "Pobierz Word (.docx)" → open the downloaded file in Word → confirm page 2 shows Jagienka Pieprzowska's footer.
</next_action>
